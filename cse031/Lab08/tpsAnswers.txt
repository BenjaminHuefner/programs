TPS 1

TPS 2
    1. I think it will print 29. I was right.
    5. The returned value will be in $v0, so we should move it to $a0 before printing
    7. 3 integer values will be stored by recursion. The first thing to store is the return address
    10. We need to store $a0 on the stack because we need to use it for both recursive calls, so we need to make sure it doesn't get 
    overrided.
    12. If we call recursion right away, the returned value will get overridden, so we need to save it to the stack before calling.
    15. The value is the return address